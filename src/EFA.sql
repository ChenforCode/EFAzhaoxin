create database EFAmembers DEFAULT CHARACTER SET gbk;

use EFAmembers;

CREATE TABLE newMembers(
  sname VARCHAR(30),
  sno VARCHAR(30),
  sclass VARCHAR(30),
  stel VARCHAR(30),
  sqq VARCHAR(30),
  sfir VARCHAR(30),
  ssec VARCHAR(30)
);